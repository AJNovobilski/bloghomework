<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <div class="container">
  <h1 class="text-left">{{title}}</h1>
  <br>
    <h5 class="text-left">{{postcontent}}</h5>
    
  </p>
</div>
    <button class="btn btn-primary">Leave A Comment</button>
  </p>
</div>
